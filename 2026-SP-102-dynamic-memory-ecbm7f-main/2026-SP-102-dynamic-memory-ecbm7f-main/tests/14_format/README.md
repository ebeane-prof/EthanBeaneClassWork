This test is checking the formatting of your code against the clang-format autoformatting tool. Use the following command to clang-format a code file:

  `clang-format -i <path_to_file>`

You may want to save this test for last, every time you modify your code you'll likely break the formatting.
